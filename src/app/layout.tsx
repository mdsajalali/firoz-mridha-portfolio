import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Firoz Mridha",
  description: "Generated by Tech Sand",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
